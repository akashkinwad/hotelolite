$('.plans-table').html('<%= j render("index")  %>');

var monthly_yearly = '<%= @monthly_yearly%>';

if (monthly_yearly == 'yearly') {
  $(".monthly").hide();
}

if (monthly_yearly == 'monthly') {
  $(".annual").hide();
}